# Trade 项目因果自适应改造与回测报告

## 1. 结论

本轮没有改写已经在 2025-04～2026-06 验证过的 Quant Fusion 牛市引擎，
而是在其外部新增了一个可审计的“部署路由层”。路由只使用回测起点之前的
固定指数和股票池数据：趋势确认时完整复用原引擎；未确认趋势时，改用低换手的
正动量龙头持有策略；证据不足时持币。

2024 年核心验收结果如下。

| 验收集 | 组合数 | 原策略盈利率 | 新策略盈利率 | 原收益中位数 | 新收益中位数 | 新回撤中位数 | 新最差回撤 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 确定性开发池 | 16 | 43.75% | **87.50%** | -2.63% | **22.35%** | 16.28% | 21.66% |
| 随机开发池，种子 20260802 | 50 | 36.00% | **90.00%** | -3.82% | **25.74%** | 14.24% | 23.93% |
| 最终冻结盲测，种子 20260805 | 26 | 未用于调参 | **96.15%** | 未用于调参 | **24.84%** | 16.28% | 31.68% |

最终盲测 25/26 严格盈利，26/26 不亏。唯一未盈利组合在选择边界没有任何
正 240 日动量标的，因此持币，收益、回撤和交易次数均为 0。

五个冻结牛市组合的收益、最大回撤和交易次数与 `main` 基线逐项严格相同，
机器结果中的 `bull_strict_invariant` 为 `true`。

## 2. 为什么不继续调原趋势参数

原引擎在 AI 主升段非常强，但 2024 横盘/弱市的主要损耗不是单个止损阈值，
而是以下结构性问题：

1. 多袖套趋势策略在假突破环境中重复入场、退出，交易中位数约 185～187 次；
2. 大股票池最多六个候选，弱股会稀释少数真正领涨股；
3. 用上一阶段哪个趋势原型最好来预测下一阶段，跨股票池没有稳定预测力；
4. 简单超跌均值回归在该 AI 硬件样本中更多遇到趋势延续，而非稳定反弹；
5. 统一组合硬止损会在龙头正常波动中提前清仓，破坏后续主升收益。

因此，本轮将“何时沿用牛市引擎”和“弱市持有哪些标的”拆开，而不是在原策略上
继续堆叠相互干扰的参数。

## 3. 新架构

### 3.1 路由层与执行层分离

- `quant_fusion.py` 保持不变，是冻结的趋势执行层；
- `regime_adaptive.py` 负责部署前证据、路由、弱市候选选择和弱市策略；
- `run_regime_validation.py` 只负责可复现验证，不被日常信号路径导入；
- `daily_signal_scan.py` 默认使用自适应路由，并把完整证据写入 JSON；
- `historical_data` 独立保存 2022～2026 验证快照，不覆盖原 `market_data`。

这种结构避免为了修复弱市而触碰已经冻结的牛市状态机，也使后续增加新的市场状态
或候选策略时，不需要修改核心成交、费用、T+1、容量和账本代码。

### 3.2 自动路由规则

部署边界默认为回测/信号起点的前一日。

1. 固定非交易指数 `000300` 和 `000682` 均有新鲜、完整的 60 日历史；
2. 两个指数均满足 MA20 > MA60：使用 `frozen_trend_engine`；
3. 否则：使用请求股票池在边界前的 240 个交易日做横截面选择；
4. 只保留 240 日收益严格大于 0 的标的，按收益降序、代码升序确定性排序；
5. 最多选 3 只；没有合格标的则使用 `cash_preservation`。

指数并不因为用户增加或删除股票而改变，因而市场状态证据具有组合成分不变性。
股票排名只在用户给出的 AI 标的池内进行，不会偷偷引入池外股票。

### 3.3 弱市交易规则

- 选择周期首次可交易开盘建仓；
- 单票目标权重不超过 59%，总权重不超过 100%；
- 浮盈未达到 30% 前不使用紧跟踪止损，避免正常波动误杀；
- 历史最高收盘浮盈达到 30% 后，启用 `最高收盘 - 3 × ATR` 吊灯退出；
- 退出后本选择周期不重新追入；
- 买卖信号在收盘后形成，最早在下一可交易开盘执行。

弱市交易仍沿用原引擎的整手、佣金、最低佣金、印花税、滑点、涨跌停、T+1、
前 20 日成交容量和单账户资金约束。没有通过取消交易成本或使用同收盘价成交来
美化结果。

## 4. 因果性和防过拟合

### 4.1 时间边界

- 市场状态和股票选择都先将数据截断到 `selection_boundary`；
- 边界必须严格早于回测起点；
- 未来行即使被修改为极端价格，也不能改变冻结边界的选择结果；
- 固定指数最后观测距边界超过 10 个自然日，状态记为 `unknown` 并失败关闭；
- 上市过晚或历史不足的股票不会使整个组合失败，只会被标记为不可用。

### 4.2 样本分层

- 确定性开发池：1、3、5、8、12、16 只不同规模及头部/分散/尾部组合；
- 随机开发池：种子 `20260802`，50 组，规模为 1、3、5、8、12；
- 最终盲测：种子 `20260805`，25 组规模为 2、4、6、10、14，另加 16 只全池
  控制样本。其规模与开发随机池不同，避免完全重复组合；
- 最终盲测只在规则、参数和专项测试冻结后首次打开，结果没有用于反调参数。

现在代码中公开了盲测种子和生成算法，因此以后重跑是“复现既有盲测”，不再是
新的未见样本。后续版本若继续优化，必须使用新的、未打开的数据边界和种子。

## 5. 数据

### 5.1 快照内容

- 16 只在两个项目数据中重叠的 A 股 AI 硬件/半导体标的，股票价格为前复权；
- 沪深 300 `000300` 与科技风险指数 `000682`，指数不复权且不参与交易；
- 共 18 个 CSV；股票最早从 2022-01-04 开始，IPO 标的保留真实较晚起点；
- 原项目 2024 年以后 `market_data` 的公共尾部保持为权威数据；
- 历史前缀来自已审计的东方财富/腾讯前复权快照；公共日期 OHLC 在拼接前比较；
- `SHA256SUMS` 固定 CSV 字节，`MANIFEST.json` 保存行数、日期、拼接行数、
  成交量单位缩放和公共区间最大价差。

### 5.2 为什么 2022 持币

当前快照没有 2021 年股票历史，无法在 2022 年初合法形成 240 日动量。
系统没有把回看期临时缩短，也没有使用 2022 年内未来数据，而是 16/16 组合持币：
收益 0、回撤 0、交易 0。这证明的是“证据不足时不亏损地失败关闭”，不是
“2022 年主动交易获得超额收益”。

## 6. 分年度和分样本结果

### 6.1 2024 确定性组合

| 指标 | 原策略 | 新路由 |
|---|---:|---:|
| 严格盈利比例 | 43.75% | **87.50%** |
| 不亏比例 | 43.75% | **100.00%** |
| 收益中位数 | -2.63% | **22.35%** |
| 最差收益 | -20.18% | **0.00%** |
| 回撤中位数 | 25.17% | **16.28%** |
| 最差回撤 | 29.05% | **21.66%** |
| 交易次数中位数 | 184.5 | **6.0** |

### 6.2 2024 随机开发组合

| 指标 | 原策略 | 新路由 |
|---|---:|---:|
| 严格盈利比例 | 36.00% | **90.00%** |
| 不亏比例 | 36.00% | **100.00%** |
| 收益中位数 | -3.82% | **25.74%** |
| 最差收益 | -20.75% | **0.00%** |
| 回撤中位数 | 26.02% | **14.24%** |
| 最差回撤 | 32.33% | **23.93%** |
| 交易次数中位数 | 186.5 | **5.0** |

### 6.3 2024 最终盲测

| 指标 | 新路由 |
|---|---:|
| 严格盈利比例 | **96.15%（25/26）** |
| 不亏比例 | **100.00%（26/26）** |
| 收益中位数 | **24.84%** |
| 最差收益 | **0.00%** |
| 最好收益 | 50.07% |
| 回撤中位数 | 16.28% |
| 最差回撤 | **31.68%** |
| 交易次数中位数 | 6.0 |

### 6.4 2022、2023

| 年份 | 组合数 | 盈利率 | 不亏率 | 收益中位数 | 最差收益 | 回撤中位数 |
|---|---:|---:|---:|---:|---:|---:|
| 2022 | 16 | 0.00% | **100.00%** | 0.00% | 0.00% | 0.00% |
| 2023 | 16 | 68.75% | 68.75% | **13.97%** | -17.08% | 24.30% |

2023 年初两个指数均满足趋势条件，因此新路由按设计完整复用原趋势引擎。
这段结果不是弱市新策略的功劳，也没有为了提高“不亏率”而覆盖原有趋势收益。

### 6.5 2025-04～2026-06 牛市黄金不变量

| 股票池 | 总收益 | 最大回撤 | 交易次数 | 与 main 严格一致 |
|---|---:|---:|---:|---:|
| 1 只 | 530.894975% | -18.341367% | 24 | 是 |
| 3 只 | 1083.697333% | -17.918988% | 194 | 是 |
| 5 只 | 1115.992376% | -15.857336% | 222 | 是 |
| 13 只 | 1028.115670% | -18.407181% | 319 | 是 |
| 22 只 | 977.828440% | -16.217690% | 240 | 是 |

这里“严格一致”是 Python 数值三元组逐项相等，不是容差内近似。

## 7. 每日信号改造

每日扫描继续只做人工决策辅助，不连接券商和自动下单：

- 默认 `--deployment-mode auto`；
- 固定指数证据从 `historical_data` 读取；
- 股票 240 日证据从本次已刷新完成的 qfq 增量缓存读取；
- `deployment` JSON 字段保存模式、策略、边界、指数 MA、候选收益、请求/入选/
  不可用标的；
- 进程退出前恢复全局缓存设置，避免库调用和测试之间的状态污染；
- 原有数据过期拒绝、跨标的日期一致性、风险状态身份、原子写入、错误工件隔离、
  严格 JSON 和买入抑制语义保持不变。

## 8. 工程质量

- 用独立模块实施新职责，没有继续膨胀 290 KB 的核心引擎；
- 冻结趋势引擎零改动，降低回归面；
- 配置常量集中定义，选择证据使用不可变 dataclass；
- 路由结果可严格 JSON 序列化；
- 晚上市标的在本地回测中因果剔除，并显式记录；
- 构造器配置只允许透传到趋势引擎，弱市路径对不兼容覆盖明确报错；
- CI 将新模块纳入 ruff、Bandit、Pyright 和自动测试发现；
- 新增 13 项专项测试，完整标准库发现式套件为 204 项。

## 9. 可复现命令

```bash
cd historical_data && sha256sum -c SHA256SUMS && cd ..
python -m py_compile quant_fusion.py quant_fusion_optimizer.py \
  daily_signal_scan.py regime_adaptive.py run_regime_validation.py
python -m unittest discover -q -b
python run_regime_validation.py --workers 4
```

机器可读结果是 `regime_validation_results.json`；其中保存全部组合成分、实际入选
标的、收益、回撤、交易次数、数据 SHA-256 和验收布尔值。

## 10. 必须保留的局限

1. 最终盲测最差回撤 31.68%，不能宣称 20% 硬回撤上限；
2. 龙头 Top-3 会提高集中度，未来 AI 产业内部风格切换可能使 240 日强势失效；
3. 16 只历史池来自可获得重叠数据，仍存在幸存者和样本选择偏差；
4. 2022 结果是缺证据持币，不是主动熊市盈利；2023 仍有 5/16 组合亏损；
5. 前复权历史可能随公司行为重述，公开数据接口也可能更改；
6. 日线回测不能模拟集合竞价深度、排队、连续跌停中的保证成交；
7. “任意组合自动获得数学全局最优收益”在未知未来不可证明。本实现追求的是
   固定因果规则在多规模、多成分、开发与冻结样本上的稳健分布，而不是事后逐池
   选择最优参数；
8. 本系统只应作为每日人工决策辅助，不能直接视为投资建议或自动下单系统。

## 11. 交付文件

- `regime_adaptive.py`：生产路由与弱市策略；
- `run_regime_validation.py`：可复现验证协议；
- `regime_validation_results.json`：逐组合机器结果；
- `historical_data/`：冻结历史数据、清单与校验和；
- `test_regime_adaptive.py`：专项测试；
- `daily_signal_scan.py`：每日自动路由与审计字段；
- `README.md`、本报告：使用说明、方法和限制；
- `.github/workflows/ci.yml`：新模块质量门禁。
