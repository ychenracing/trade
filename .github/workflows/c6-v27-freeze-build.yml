name: C6 v27 Freeze Build

on:
  push:
    branches: [codex/c6-v27-freeze-build-20260908]
    paths:
      - .github/scripts/build_c6_v27_from_v26.py
      - .github/workflows/c6-v27-freeze-build.yml

permissions:
  actions: read
  contents: write

concurrency:
  group: c6-v27-freeze-build-20260908
  cancel-in-progress: false

jobs:
  freeze:
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12.14'
          cache: pip
          cache-dependency-path: requirements-lock.txt
      - name: Verify frozen-style interpreter
        run: test "$(python -c 'import platform; print(platform.python_version())')" = '3.12.14'
      - name: Install locked dependencies
        run: python -m pip install --require-hashes -r requirements-lock.txt
      - name: Build immutable W27 and derive v27 freeze builder
        run: |
          python -m py_compile .github/scripts/build_c6_v27_from_v26.py
          python .github/scripts/build_c6_v27_from_v26.py
          python -m py_compile /tmp/build_c6_v27_freeze.py
      - name: Build validate and atomically publish P27 I_B27 I_S27 R27
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: python /tmp/build_c6_v27_freeze.py
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with:
          name: c6-v27-freeze-receipt-${{ github.run_id }}
          path: |
            c6-v27-freeze-receipt.json
            c6-v27-anchor-receipt.txt
          if-no-files-found: ignore
          retention-days: 90
