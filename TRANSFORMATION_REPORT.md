# 收益、回撤与交易次数优化改造报告

**仓库：** `ychenracing/trade`

**改造日期：** 2026-08-07

**目标分支：** `main`
**生产基线：** 200 万元，2025-04-01 至 2026-07-20，前复权冻结日线，预热模式

## 一、验收原则

本轮优先提高收益、降低回撤、减少交易次数，同时禁止用未来数据、重置账户、统一三个策略账本或永久降低牛市仓位来换取表面结果。新增机制默认自动工作，但必须遵守单一风险执行者原则，避免多个层级对同一证据重复减仓。

历史回测不是收益承诺。固定股票池和单一牛市阶段不能证明未来有效，压力场景也显示任意子集可能超过 20% 回撤。

## 二、最终生产架构

生产路径由三个持续层组成：

1. `ProductionReplayEngine` 逐日生成外层路由，但始终调用同一个 `BacktestEngine`、同一套开盘撮合和同一个账户账本；
2. fast/base/slow 三个袖套保留独立持仓、现金、策略、挂单和风险峰值，只迁移未使用现金；
3. `CrossMarketOverlay` 从独立风险篮子更新连续风险状态，并在已有趋势账本中成为唯一外部风险执行者。

空仓进入 `weak` 时，生产路由才建立正动量龙头账本；已有趋势账本进入弱状态时不清仓、不新建重复账本，由原策略退出逻辑和跨市场叠加层继续管理。专用弱市或现金账本运行时，叠加层保持测量和恢复状态，但不重复卖出、冷却或阻断。

## 三、最高优先级改造

### 3.1 生产逐日回放

新增 `ProductionRouteController` 与 `ProductionReplayEngine`：

- 逐日复用生产 ensemble 路径，不在状态边界拼接多段净值；
- 审计保存每日路由、切换事件、袖套现金、持仓、挂单数、终端锁、风险峰值、粘性龙头和弱市冷却；
- 修复预热状态机只从请求开始日推进的错误，现从预热历史推进状态、只在请求区间输出；
- `auto` 与 `replay` 默认使用生产逐日回放，显式 `trend` 与 `weak` 只保留为研究边界实验；
- 恢复路由需要 40 个连续交易日确认，期间继续持有弱市正动量龙头，不是空仓等待；
- 弱市周期冻结领导股集合，避免每日重排造成隐性轮动。

### 3.2 连续跨市场风险升级

风险层从固定持仓篮子改为独立的 23 股、子行业等权篮子，覆盖光通信、存储、算力、设备和材料。风险等级支持三种因果路径：

- 预警、恢复、再次冲击；
- 连续多日恶化，从一级自动进入二级；
- 严重收益、广度和均线破坏同时成立时直达更高等级。

一级冻结加仓；二级冻结新标的并削减最弱非核心仓；三级进一步降风险。持续内部 `TRANSITION` 只有同时存在外部一级风险、账户回撤和当前下跌时，才允许一次小比例削减。多种风险卖出按优先级收敛，避免同日重复卖出同一策略账本。

### 3.3 过渡期风险预算

新增 `regime_transition_pyramid_scale` 与 `regime_transition_trim_confirmations`。默认值保持牛市无损：单独的内部过渡状态不自动降低总风险；只有跨市场层提供第二份独立风险证据时才冻结加仓、限制新仓或裁剪最弱仓。`CHOPPY` 仍可使用更低持仓上限和更慢袖套资金权重。

早期实验曾把所有 `TRANSITION` 全局缩仓，导致 22 股收益降至约 770%、交易增至约 298 笔，因此被否决。最终设计把风险动作限定在双证据路径。

### 3.4 三目标优化与推广门

`quant_fusion_optimizer.py` 不再只按收益和回撤选择：

- Pareto 目标同时包含年化收益、最差回撤和验证交易次数；
- 排序先按约 2 个百分点收益档分层，再比较回撤、交易和稳健得分；
- 普通及压力 holdout 的财富不得低于基线 99%；
- 回撤不得比基线恶化超过 0.5 个百分点；
- 交易不得增加超过 3%，除非财富至少提高 5%；
- 报告记录收益、回撤和交易的全部差值及每一项门禁结果。

### 3.5 分阶段参数搜索

新增 `--stage risk|turnover|return|all`：

- 风险阶段只搜索连续确认、二三级回撤、过渡缩放和风险梯度；
- 换手阶段只搜索粘性门槛、确认日、轮换周期、冷却和候选分位；
- 收益阶段才搜索入场、出场、风险预算和动态袖套画像。

各阶段隔离参数族，避免用收益参数掩盖回撤退化，或用风险参数掩盖交易膨胀。优化缓存和最终报告继续使用临时文件、刷盘和原子替换。

### 3.6 股票池压力体系

`stress_test_prefixes.py` 现在生成：

- 1 至 22 股全部前缀；
- 每只股票一次留一；
- 从 5/9/13 股基池逐一加入剩余股票；
- 3/5/8/12/16 股随机子集，默认每个规模 50 次；
- 完整 22 股顺序置换，默认 50 次。

汇总同时给出收益中位数、10 分位和最差值，回撤中位数、10 分位和最差值，交易中位数、90 分位和最高值。`prefix_stress.json` 与 `universe_stress.json` 只有全部场景成功后才原子替换。

## 四、次优先级改造

### 4.1 动态袖套现金权重

`dynamic_sleeve_weights=True` 默认开启。内部状态确认改变后，只对三个袖套的空闲现金按 fast/base/slow 权重重新分配；持仓、策略、挂单、风险峰值和冷却均保持独立。趋势状态恢复等权，过渡和震荡状态偏向 base/slow。

该机制对不同股票池影响非线性：它明显提高 5/22 股收益，但压低 13 股收益。最终默认选择跨股票池整体更稳健的权重，不针对 13 股单池过拟合。

### 4.2 趋势健康盈利保护

盈利分层保护只在确认跨市场风险时读取趋势健康。价格高于上升的 MA20/MA60 时多给 3 个百分点回吐空间，价格跌破弱化均线时少给 3 个百分点；调整受 14% 最小宽度和 28% 灾变地板约束。关闭该功能不会改变没有确认风险的干净牛市。

### 4.3 自适应候选与持仓数

新增可配置粘性评分差、连续确认日、轮换周期和被换出冷却。自适应规则只在外部风险至少一级时收紧，正常趋势保持原持仓容量。组合持仓数可在 3 至 6 之间变化：`CHOPPY` 默认最多 3，`TRANSITION` 默认最多 4，干净趋势最多 6。

### 4.4 真实行业相对强度

`account_signal_engine.py` 的候选评分补齐 15% 行业相对强度。每个候选的 20/60 日收益与其细分画像组均值、本次请求的 AI 候选池均值比较，再映射到 0 至 1。此前文档声明了这 15% 权重但代码没有实际输入，现有单元测试验证强弱行业会产生准确的 0.15 分差。

### 4.5 成熟与新兴龙头

弱市候选保留成熟通道，并增加最少 61 日历史的新兴通道。成熟通道使用 240 日动量、120 日相对强度、60 日动量、回撤韧性和趋势修复；新兴通道使用 60/20 日动量、突破质量、成交量扩张和趋势修复。每周期最多一只纯新兴龙头，防止短历史样本挤占成熟核心。

## 五、回测结果

### 5.1 五个生产股票池

| 股票数量 | 总收益 | 最大回撤 | 交易次数 |
|---:|---:|---:|---:|
| 1 | 530.8950% | -18.3414% | 24 |
| 3 | 1122.4308% | -17.1850% | 176 |
| 5 | 1212.2638% | -15.6716% | 233 |
| 13 | 989.7927% | -17.6920% | 234 |
| 22 | 1066.5430% | -16.9115% | 251 |

相对旧文档基线：3/5/22 股收益提高；5/13/22 股回撤改善；3/5/13 股交易减少。13 股总收益少 44.48 个百分点，但最终财富只下降约 3.9%，同时回撤改善约 0.99 个百分点、交易少 6 笔。旧文档的 22 股 244 笔无法由旧代码复现；旧代码实际为 252 笔，新版为 251 笔。

### 5.2 熊市与牛市生产回放

| 场景 | 总收益 | 最大回撤 | 交易次数 |
|---|---:|---:|---:|
| 2024 五股弱市 | 54.3787% | -17.2195% | 15 |
| 2025—2026 一股牛市 | 530.8950% | -18.3414% | 24 |
| 2025—2026 五股牛市 | 1212.2638% | -15.6716% | 233 |

牛市生产回放与趋势基线逐位一致，证明逐日路由没有重复缩仓。熊市回放相对旧冻结研究点约 59.19%、-18.79%、15 笔，收益最终财富下降约 3.0%，回撤改善约 1.58 个百分点，交易不增。

### 5.3 压力场景

本次持久化工件包含 89 个场景，完成全部前缀、留一和逐一加入，并对五个随机规模和顺序置换各做一次烟雾样本。默认脚本仍为各 50 次随机抽样。

| 场景族 | 数量 | 最差收益 | 最差回撤 | 最高交易 |
|---|---:|---:|---:|---:|
| 前缀 | 22 | 530.8950% | -19.1710% | 273 |
| 留一 | 22 | 673.0675% | -19.2553% | 336 |
| 逐一加入 | 39 | 800.2432% | -19.2099% | 355 |
| 随机子集烟雾 | 5 | 502.0890% | -23.0236% | 309 |
| 顺序置换烟雾 | 1 | 1066.5430% | -16.9115% | 251 |

最差随机 8 股场景相对旧代码把回撤从 -23.92% 改善到 -23.02%，交易从 368 降到 309，但最终财富下降约 5.7%。这属于重要限制，不能声称所有任意子集都满足 18% 回撤目标。

## 六、被否决的方案

以下方案经过真实回测后未进入默认配置：

- 对所有 `TRANSITION` 永久缩仓：显著降低 22 股收益并增加交易；
- 把弱市策略复制到三个袖套：重复账本导致回撤和交易恶化；
- 进入弱市时清空已有趋势持仓：牛市回放收益大幅下降；
- 每日重新选择弱市龙头：形成隐性轮动；
- 动态持仓上限在无外部风险时生效：破坏正常牛市路径；
- 把风险篮子限制为用户当前股票池：导致风险证据随输入集合漂移；
- 为 13 股单池调成近似等权动态现金：13 股收益提高，但 22 股收益、回撤和交易明显恶化；
- 重新启用真实账户历史注入：混合账本、时间和挂单状态仍不安全，继续显式拒绝。

## 七、代码、测试与文档一致性

新增或更新的覆盖包括：连续风险升级、独立风险篮子、生产路由执行权、弱市监测模式、生产牛熊回放、阶段参数空间、交易三目标 Pareto、压力场景确定性、账户行业相对强度、pandas 3 类型兼容和 JSON 严格序列化。

维护模块 Pyright 已从现有错误修到零错误；Ruff、编译、完整单元测试、Bandit、依赖审计和五池精确回归均作为最终发布门禁。`README.md`、`BACKTEST_RESULTS.md`、本文件、黄金指标和回测工件使用同一组数字。

## 八、最终判断

本轮落实了共享审查中的生产逐日回放、连续风险升级、双证据过渡风控、三目标优化、分阶段搜索、股票池压力、动态袖套、趋势健康、自适应候选、独立行业篮子和账户行业相对强度。固定五池和 2024 熊市结果满足“不出现大幅负面影响”的验收原则，且多数池同时改善收益、回撤或交易。

仍需明确保留两项限制：13 股最终财富小幅下降约 3.9%；随机子集烟雾测试仍出现 -23.02% 回撤。代码不会把这些结果包装成收益保证，后续优化应使用更多年份、完整 50 次随机分布和真实人工执行对账，而不是继续针对当前五池调参。
