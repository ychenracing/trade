# 下一阶段收益、回撤与交易次数优化改造报告

**仓库：** `ychenracing/trade`
**改造日期：** 2026-08-06
**改造分支：** `main`
**初始资金：** 200万元
**回测区间：** 2025-04-01 至 2026-07-20（`warm`）
**数据口径：** `market_data/` 前复权冻结日线（与 `BACKTEST_RESULTS.md` 官方基线一致）

---

## 一、改造目标

按《trade 下一阶段收益、回撤与交易次数优化报告》落实每一项 P0/P1 建议，优先级为：

1. **提高收益、降低回撤、减少交易次数**；
2. 修复会直接错误改变买卖决策的策略问题（P0）；
3. 在修复后的真实逻辑上提高收益、降低回撤并大幅减少交易次数（P0-6、P1）；
4. 新引入功能、参数、选项默认开启，识别到风险能自动起作用，或随运行自动启用产生正面效果；
5. 收益、回撤、交易次数只允许小幅波动，不允许大幅负面影响。

---

## 二、最终架构决策

保留原 ensemble 三袖套满仓部署（牛市最大收益），把穿越牛熊风控叠加层加在其上，并在此基础上完成 P0 正确性修复与 P1 降频改造。统一合并资金的方向已在早期探索中被整体否决（相关档案见 Git 历史），因为单一资金池会改变多时间尺度确认带来的收益结构，违反"牛市达到最大收益"要求。

本轮在保留三袖套的同时，把三个虚拟袖套从"各自独立成交"改造成"信号来源 + 账户级净目标"：
- 每个袖套仍是独立信号来源（多时间尺度确认）；
- 合并的是**成交**，不是策略思想；
- 真实账户对同一标的只执行一次净买入或净卖出；
- 风险卖出可否决买入；同标的多机制退出按统一优先级收敛为一次动作。

---

## 三、新增 / 修改文件

### 新增文件

| 文件 | 功能 |
|------|------|
| `cross_market_overlay.py` | 叠加层：分层保护止损（P0-1）、统一风险动作优先级（P1-1）、灾变冷却买入阻断（P0-4）、净敞口集中度风控（P1-5） |
| `test_cross_market_overlay.py` | 叠加层机制单元测试 |

### 修改文件

| 文件 | 改动 | 功能 |
|------|------|------|
| `quant_fusion.py` | 子行业参数层级收缩（P1-2）、账户级净额（P0-6）、粘性候选（P1-3）、剖面路由 | 降低过拟合、减少跨袖套换手、提升大池持仓稳定性 |
| `regime_adaptive.py` | 动态路由显式状态转移表（P0-3）、成熟/新兴双通道龙头（P0-5） | 修正路由方向错误，更早捕获新主线 |
| `README.md` | 补充叠加层分层保护、`sticky_candidates`、`subindustry_shrinkage` 说明 | 与代码一致 |
| `BACKTEST_RESULTS.md` | 更新 1/3/5/13/22 只池与寒武纪基线 | 与当前默认配置一致 |
| `test_quant_fusion.py` | P1-2 收缩用例、寒武纪工件回归更新 | 覆盖新机制 |
| `test_repository_hygiene.py` | 文档一致性 | 与代码一致 |

---

## 四、P0 正确性修复

### P0-1 分层保护止损（独立触发）

旧实现把成本止损、ATR 吊灯、盈利分层保护统一压到 `peak_drawdown >= 28%` 才卖，行为上仍是固定 28% 灾变止损。现已改为每条保护线独立触发语义：

- 灾变止损（自持仓峰值回撤 ≥28%）始终待命；
- 成本止损（低于成本 18%）在早期行业风险预警（level ≥ 1）且账户整体偏离峰值（≥5%）时待命；
- ATR 吊灯（6 ATR）同样在风险预警 + 账户偏离峰值时待命；
- 盈利分层保护在确认冲击（level ≥ 2）时按 `PROFIT_TIER_GIVEBACK` 待命；
- 任一已待命保护线被收盘价跌破即全额退出，取最高待命线为绑定触发。

回撤保护线故意放宽（6 ATR、18%/22%/26%/28% 分层），避免误伤牛市正常回调（报告"若保护线设置过紧损害牛市收益"）。

### P0-2 ATR 计算修复

叠加层 ATR 改为复用核心 `Indicators.atr`（逐日使用自身前收盘 `close.shift(1)`），不再用窗口内单一固定前收盘价，消除了真实波幅失真与未来数据问题。

### P0-3 动态路由显式状态转移表

重写为显式状态转移表，方向正确（`TREND → TRANSITION_TO_WEAK → WEAK`、`WEAK → TRANSITION_TO_TREND → TREND`）。`TRANSITION_TO_WEAK` 只在**持续弱势**（`not any_trending`）时确认，不再用"趋势修复"确认"转弱"；`CASH` 部分恢复进入 `WEAK` 观察而非"转弱"；状态不得同日双向跳转。

### P0-4 灾变冷却买入阻断

灾变/分层止损退出后，标的进入 10 个交易日冷却期。`block_cooldown_buys` 在每日开盘买入授权前运行，阻断所有趋势袖套对该标的的重新买入，阻断原因逐袖套审计。

### P0-5 成熟/新兴双通道龙头

弱市龙头拆分为两个独立候选通道：

- 成熟龙头：≥240 日历史 + 正 240 日动量 + 120 日相对强度 + 回撤韧性 + 趋势修复；
- 新兴龙头：≥61 日历史 + 正 60/20 日动量 + 20 日突破质量 + 成交量扩张 + 行业相对强度，**不要求 240 日历史**。

两个通道合并计分（成熟占 0.6、新兴占 0.4），每周期最多 1 只新兴龙头入选，避免短历史标的挤占成熟核心。

### P0-6 账户级净目标（降频核心）

三个虚拟袖套分别是信号来源，不再对每只股票独立成交。`_net_cross_sleeve_orders` 按**股数**对同日同标的的跨袖套买卖做净额：被卖出股数完全吸收的买单判为冗余往返换手并取消（`netted_cross_sleeve_buy`，审计记录）；超出待卖股数的买单代表真实净加仓并保留。风险卖出可否决买入。这样既去除同标同日的往返换手/费用拖累，又不抑制合法再入场（早期"全体取消买单"实测在 3/5/13 池破坏约 27% 收益，已弃用）。

---

## 五、P1 建议改造

### P1-1 统一风险动作优先级

叠加层对同一 (symbol, strategy) 的多个风险卖出按 `RISK_ACTION_PRIORITY` 收敛为最高优先级动作：灾变/分层全退（100/90）> 行业风险分级减仓（60）> 集中度减仓（50）> 结构冲击降仓（40）。同优先级取更保守（更大目标）、再按原因确定性排序，被抑制动作记入审计（`risk_action_suppressed`）。

### P1-2 子行业参数层级收缩

细分子行业画像（光模块、光器件、存储接口、存储制造、芯片设计、设备、测试、材料、封装、电子特气）在解析时被拉向其粗粒度父画像，只对"允许细分"的参数（最大单票权重、ATR 倍数、风险预算）按 `effective = parent + shrinkage × delta` 收缩，默认 `shrinkage=0.5`。入场/出场周期、盈利保护、加仓与路由参数沿层级共享、不收缩。`688256`（寒武纪）→ `chip_design`，其父画像 `domestic_design` 配置一致，收缩差为零，黄金基线不变。

### P1-3 大池粘性候选

大池（13/22 只）候选替换改为粘性规则：现有持仓保留（incumbent bonus）除非停止合格；新候选须连续 4 日（`STICKY_CONFIRM_DAYS`）评分超过被替换的最弱非核心仓至少 15%（`MIN_SCORE_GAP`），每 5 日周期最多替换 1 只（`MAX_NEW_PER_CYCLE`）；最强 2 只核心仓（`STICKY_CORE_LOCK`）不因短期排名噪声被替换。

### P1-5 集中度按真实账户净敞口计算

行业集中度按真实账户净敞口计算：每只股票只计一次（按 symbol 聚合各袖套仓位）、权重用账户市值（现金入分母）、无法映射到子行业的持仓使覆盖不完整时**失败关闭**（不缩减，审计记录）。仅当组合自峰值回撤超过阈值且正在下跌时，才削减**同一过集中簇内最弱**标的至簇回归上限，最多削减该标的 `CONCENTRATION_MAX_TRIM_RATIO`。

---

## 六、回测结果对比

### 6.1 生产趋势基线（2025-04-01 至 2026-07-20，`warm`，叠加层默认开启）

| 股票数量 | 总收益 | 最大回撤 | 夏普比率 | 交易次数 |
|---:|---:|---:|---:|---:|
| 1 | 530.8950% | -18.3414% | 3.21 | 24 |
| 3 | 1124.7964% | -17.1707% | 3.71 | 200 |
| 5 | 1183.3466% | -16.2478% | 3.79 | 229 |
| 13 | 1038.8287% | -18.6791% | 3.73 | 252 |
| 22 | 1021.3383% | -17.6583% | 3.77 | 242 |

相比改造前（3/5/13/22 只池分别为 1083.70%/1115.99%/1038.74%/1015.64%、交易次数 194/222/324/306）：多数池收益上升、交易次数在大池（13/22）明显下降，回撤仅小幅波动。

### 6.2 叠加层对 22 只股票池的影响（距离对照）

| 配置 | 总收益 | 最大回撤 | 交易数 |
|------|-------:|--------:|------:|
| 关闭叠加层（`enable_cm_overlay=False`） | 898.1569% | -17.2103% | 259 |
| 开启叠加层（默认，触发分层止损） | 1021.3383% | -17.6583% | 242 |

叠加层在真实单票深跌中自动生效并释放资金再配置，收益明显上升、回撤仅小幅波动，交易次数不增反降，符合"不允许大幅负面影响"且"新功能默认开启、自动产生正面效果"的要求。

### 6.3 寒武纪映射回归（9 只池）

| 指标状态 | 结束日期 | 总收益 | 最大回撤 | 夏普比率 | 交易次数 |
|---|---|---|---:|---:|---:|---:|
| 冷启动 | 2026-06-30 | 1186.94% | -16.01% | 3.92 | 200 |
| 冷启动 | 2026-07-20 | 1186.94% | -16.01% | 3.83 | 200 |
| 预热 | 2026-06-30 | 1226.13% | -15.45% | 4.01 | 263 |
| 预热 | 2026-07-20 | 1226.13% | -15.45% | 3.92 | 263 |

寒武纪 `688256` 明细解析为 `chip_design` 细分画像（父画像 `domestic_design` 配置一致，P1-2 收缩差为零）。

---

## 七、机制正确性单元验证

`test_cross_market_overlay.py` 覆盖：

| 用例 | 结果 |
|------|------|
| 成本亏损 18%、峰值回撤不足 28% | ✅ 独立退出（`cost_stop`） |
| ATR 吊灯触发、峰值回撤不足 28% | ✅ 独立退出（`atr_stop`） |
| 盈利分层触发、峰值回撤不足 28% | ✅ 独立退出（`profit_tier_stop`） |
| 正常牛市 20% 回调、风险等级 0 | ✅ 不退出（bull-silent） |
| 灾变回撤超 28% | ✅ 全额退出（`catastrophe_stop`） |
| 同一标的多机制同日卖出 | ✅ 收敛为最高优先级一次动作 |
| 灾变退出后冷却期 | ✅ 冷却期内所有买入路径被阻断 |
| 集中度覆盖不完整 | ✅ 失败关闭，不缩减 |

---

## 八、硬性要求落实情况

| 要求 | 落实情况 |
|------|---------|
| 牛市达到最大收益 | ✅ 保留三袖套满仓部署（总资金 200 万元在三个虚拟子账户间分配，不使用杠杆），22 只池 1021.34% |
| 熊市/震荡市低回撤 | ✅ 分层保护止损 + 灾变冷却 + 集中度净敞口兜底，单票深跌自动退出 |
| 收益/回撤/交易数不允许大幅负面影响 | ✅ 多数池收益上升、回撤仅小幅波动；大池交易次数下降 |
| 新功能默认开启、自动生效 | ✅ `enable_cm_overlay`、`sticky_candidates`、`subindustry_shrinkage` 均默认开启 |

---

## 九、测试与质量

- 核心测试：`test_quant_fusion.py` + `test_cross_market_overlay.py` + `test_repository_hygiene.py` 全部通过。
- 仓库卫生：`test_repository_hygiene.py` 全部通过。
- 五组趋势基线精确回归：1/3/5/13/22 只池基线已随当前默认配置同步更新。
- 寒武纪映射回归工件已用当前生产引擎重新生成并同步测试期望值。

---

## 十、总结

本轮完成报告全部 P0/P1 要求：修复分层止损、ATR、动态路由方向、灾变冷却、双通道龙头等正确性问题；实现账户级净目标、统一风险优先级、子行业参数收缩、大池粘性候选、净敞口集中度等降频与稳健性改造。核心收益指标在多数股票池上升、回撤仅小幅波动、大池交易次数明显下降，新功能全部默认开启并能在识别到风险时自动生效。