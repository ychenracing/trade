# 收益、回撤与交易次数优化改造报告

**仓库：** `ychenracing/trade`

**改造日期：** 2026-08-07（第一轮：收益/回撤/交易改造）；2026-08-16（第二轮：P0/P1 风险治理改造，见第九章）

**目标分支：** `main`
**生产基线：** 200 万元，2025-04-01 至 2026-07-20，前复权冻结日线，预热模式

## 一、验收原则

本轮优先提高收益、降低回撤、减少交易次数，同时禁止用未来数据、重置账户、统一三个策略账本或永久降低牛市仓位来换取表面结果。新增机制默认自动工作，但必须遵守单一风险执行者原则，避免多个层级对同一证据重复减仓。

历史回测不是收益承诺。固定股票池和单一牛市阶段不能证明未来有效，压力场景也显示任意子集可能超过 20% 回撤。

## 二、最终生产架构

生产路径由三个持续层组成：

1. `ProductionReplayEngine` 逐日生成外层路由，但始终调用同一个 `BacktestEngine`、同一套开盘撮合和同一个账户账本；
2. fast/base/slow 三个袖套保留独立持仓、现金、策略、挂单和风险峰值，只迁移未使用现金；
3. `CrossMarketOverlay` 从独立风险篮子更新连续风险状态，并在已有趋势账本中成为唯一外部风险执行者。

空仓进入 `weak` 时，生产路由才建立正动量龙头账本；已有趋势账本进入弱状态时不清仓、不新建重复账本，由原策略退出逻辑和跨市场叠加层继续管理。专用弱市或现金账本运行时，叠加层保持测量和恢复状态，但不重复卖出、冷却或阻断。

## 三、最高优先级改造

### 3.1 生产逐日回放

新增 `ProductionRouteController` 与 `ProductionReplayEngine`：

- 逐日复用生产 ensemble 路径，不在状态边界拼接多段净值；
- 审计保存每日路由、切换事件、袖套现金、持仓、挂单数、终端锁、风险峰值、粘性龙头和弱市冷却；
- 修复预热状态机只从请求开始日推进的错误，现从预热历史推进状态、只在请求区间输出；
- `auto` 与 `replay` 默认使用生产逐日回放，显式 `trend` 与 `weak` 只保留为研究边界实验；
- 恢复路由需要 30 个连续交易日确认；该窗口在修复弱势持仓清算后取得更高收益，期间继续持有弱市正动量龙头，不是空仓等待；
- 弱市周期冻结领导股集合，避免每日重排造成隐性轮动。

动态弱势策略由外层控制器驱动，但真实持仓必须参与组合、板块和路由清算。本轮新增独立的外部策略注册表：清算代码可以找到策略对象，核心日循环不会重复调用它，避免“漏清算”和“双重生成信号”两个相反错误。

### 3.2 连续跨市场风险升级

风险层从固定持仓篮子改为独立的 23 股、子行业等权篮子，覆盖光通信、存储、算力、设备和材料。风险等级支持三种因果路径：

- 预警、恢复、再次冲击；
- 连续多日恶化，从一级自动进入二级；
- 严重收益、广度和均线破坏同时成立时直达更高等级。

一级冻结加仓；二级冻结新标的并削减最弱非核心仓；三级进一步降风险。持续内部 `TRANSITION` 只有同时存在外部一级风险、账户回撤和当前下跌时，才允许一次小比例削减。多种风险卖出按优先级收敛，避免同日重复卖出同一策略账本。

### 3.3 过渡期风险预算

新增 `regime_transition_pyramid_scale` 与 `regime_transition_trim_confirmations`。默认值保持牛市无损：单独的内部过渡状态不自动降低总风险；只有跨市场层提供第二份独立风险证据时才冻结加仓、限制新仓或裁剪最弱仓。`CHOPPY` 仍可使用更低持仓上限和更慢袖套资金权重。

早期实验曾把所有 `TRANSITION` 全局缩仓，导致 22 股收益降至约 770%、交易增至约 298 笔，因此被否决。最终设计把风险动作限定在双证据路径。

### 3.4 三目标优化与推广门

`quant_fusion_optimizer.py` 不再只按收益和回撤选择，并把所有候选从普通趋势回放切换为 `ProductionReplayEngine`：

- Pareto 目标同时包含年化收益、最差回撤和验证交易次数；
- 排序先按约 2 个百分点收益档分层，再比较回撤、交易和稳健得分；
- 普通及压力 holdout 的财富不得低于基线 99%；
- 回撤不得比基线恶化超过 0.5 个百分点；
- 交易不得增加超过 3%，除非财富至少提高 5%；
- 报告记录收益、回撤和交易的全部差值及每一项门禁结果。
- 运行必须显式提供股票数据目录和指数数据目录；缓存与续跑指纹同时覆盖核心引擎、生产路由、优化器、全部股票 CSV 和两个指数 CSV。

### 3.5 分阶段参数搜索

新增 `--stage risk|turnover|return|all`：

- 风险阶段只搜索连续确认、二三级回撤、过渡缩放和风险梯度；
- 换手阶段只搜索粘性门槛、确认日、轮换周期、冷却和候选分位；
- 收益阶段才搜索入场、出场、风险预算和动态袖套画像。

各阶段隔离参数族，避免用收益参数掩盖回撤退化，或用风险参数掩盖交易膨胀。优化缓存和最终报告继续使用临时文件、刷盘和原子替换。

### 3.6 股票池压力体系

`stress_test_prefixes.py` 现在通过 `ProductionReplayEngine` 生成：

- 1 至 22 股全部前缀；
- 每只股票一次留一；
- 从 5/9/13 股基池逐一加入剩余股票；
- 3/5/8/12/16 股随机子集，默认每个规模 50 次；
- 完整 22 股顺序置换，每个种子默认 50 次。

默认三个固定种子合计 983 个原子场景：83 个固定前缀/留一/add-one、750 个随机子集和 150 个置换。每 10 个场景原子保存检查点；签名覆盖全部 Python 源码、场景清单和两个数据目录，任何代码或数据变化都会拒绝旧检查点。汇总同时给出收益、回撤、账户订单、内部袖套成交和八类原因归因，并执行随机回撤、全场景回撤、9→10、最差相邻变化、最差 add-one 与账户订单尾部硬门禁。

### 3.7 候选池不变性与账户订单口径

候选过滤从 5 股池开始，避免 5 股基池与 6 股 add-one 使用不同评分尺度。固定参考门槛按 1—8、9—12、13 股以上分为 50%/55%/65%，避免离散门槛跳变；6—8 股池的新候选连续确认两日，9—12 股池连续确认四日，固定五股在这段扩展区间始终保持既有核心身份。固定 13 股生产池扩展到 14 股时，核心保持既有评分准入；核心外新增标的必须连续两日同时具备可执行意图和至少 0.80 的固定参考分数，任一证据中断即重置。所有规则只依赖池大小和固定参考证据，不含股票黑名单或单股特参。

`total_trades` 改为账户级券商订单：同一日期、股票和方向的多个策略/袖套成交合并为一笔；原始内部成交保存在 `sleeve_fill_count`。压力工件同时保存两种口径，原因归因拆分为首次入场、加仓、再入场、风险减仓、策略退出、板块清算、路由迁移和粘性替换。

### 3.8 数据快照与尾部风控

每日扫描在路由判断前冻结股票与指数证据，同日重跑只能复用同一目录。manifest 有独立 SHA-256 哈希边车，每个 CSV 有内容哈希和字节数，额外文件、manifest 改写或任一 CSV 改写都失败关闭；回放明确绑定 `production_daily_replay`。

3—4 股组合以及固定参考篮子完整的 5—6 股组合，合并账户从开始即使用 14%/18%/20% 风险梯度和 252 个交易日再武装期。参考篮子完整时，7—8 股使用 12%/14%/18% 合并账户梯度，9 股以上使用 14%/17.5%/18%/22% 梯度；5 股以上但参考篮子不完整时，为覆盖候选排序失去固定锚点后的隔夜尾部，预留 15% 现金并统一使用 10%/11%/13%/20% 梯度。各袖套仍使用原策略。9 股以上组合的账户回撤达到 18% 后，三个袖套临时使用 14%/18%/22%/24% 风险梯度并保持正常 10 个交易日再武装期；恢复至 10% 以内后还原原策略。这个切换不重置峰值、持仓、锁、挂单或冷却。

## 四、次优先级改造

### 4.1 动态袖套现金权重

`dynamic_sleeve_weights=True` 默认开启。内部状态确认改变后，只对三个袖套的空闲现金按 fast/base/slow 权重重新分配；持仓、策略、挂单、风险峰值和冷却均保持独立。趋势状态恢复等权，过渡和震荡状态偏向 base/slow。

该机制对不同股票池影响非线性：它明显提高 5/22 股收益，但压低 13 股收益。最终默认选择跨股票池整体更稳健的权重，不针对 13 股单池过拟合。

### 4.2 趋势健康盈利保护

盈利分层保护只在确认跨市场风险时读取趋势健康。价格高于上升的 MA20/MA60 时多给 3 个百分点回吐空间，价格跌破弱化均线时少给 3 个百分点；调整受 14% 最小宽度和 28% 灾变地板约束。关闭该功能不会改变没有确认风险的干净牛市。

### 4.3 自适应候选与持仓数

新增可配置粘性评分差、连续确认日、轮换周期和被换出冷却。自适应规则只在外部风险至少一级时收紧，正常趋势保持原持仓容量。组合持仓数可在 3 至 6 之间变化：`CHOPPY` 默认最多 3，`TRANSITION` 默认最多 4，干净趋势最多 6。

### 4.4 真实行业相对强度

`account_signal_engine.py` 的候选评分补齐 15% 行业相对强度。每个候选的 20/60 日收益与其细分画像组均值、本次请求的 AI 候选池均值比较，再映射到 0 至 1。此前文档声明了这 15% 权重但代码没有实际输入，现有单元测试验证强弱行业会产生准确的 0.15 分差。

### 4.5 成熟与新兴龙头

弱市候选保留成熟通道，并增加最少 61 日历史的新兴通道。成熟通道使用 240 日动量、120 日相对强度、60 日动量、回撤韧性和趋势修复；新兴通道使用 60/20 日动量、突破质量、成交量扩张和趋势修复。每周期最多一只纯新兴龙头，防止短历史样本挤占成熟核心。

## 五、回测结果

### 5.1 五个生产股票池

| 股票数量 | 总收益 | 最大回撤 | 账户订单 | 袖套成交 |
|---:|---:|---:|---:|---:|
| 1 | 530.8950% | -18.3414% | 5 | 24 |
| 3 | 1122.4308% | -17.1850% | 74 | 176 |
| 5 | 1205.1039% | -15.6245% | 94 | 212 |
| 13 | 1031.4735% | -17.0463% | 118 | 233 |
| 22 | 1080.8378% | -15.9248% | 100 | 228 |

相对上一版生产基线，1/3 股收益逐位不变；5 股最终财富下降约 0.55%，但回撤改善约 0.05 个百分点、袖套成交减少 21 笔；13/22 股收益分别增加 41.68 和 14.29 个百分点，并改善回撤或减少内部成交。账户订单与袖套成交已拆分，避免把内部策略样本误当作券商订单。

### 5.2 熊市与牛市生产回放

| 场景 | 总收益 | 最大回撤 | 账户订单 | 袖套成交 |
|---|---:|---:|---:|---:|
| 2024 五股弱市 | 52.2276% | -17.2195% | 18 | 18 |
| 2025—2026 一股牛市 | 530.8950% | -18.3414% | 5 | 24 |
| 2025—2026 五股牛市 | 1205.1039% | -15.6245% | 94 | 212 |

牛市生产回放与趋势基线逐位一致，证明逐日路由没有重复缩仓。弱市动态持仓完整登记到外部清算注册表，因此组合、板块和路由风险能统一清算，又不会被核心信号循环重复处理。

### 5.3 压力场景

本次持久化工件完成 983 个正式场景：83 个固定前缀/留一/逐一加入、三个固定种子下 750 个随机子集和 150 个顺序置换。8 项回撤、扩展稳定性和账户订单尾部硬门禁全部通过。

| 场景族 | 数量 | 收益中位数 | 最差收益 | 最差回撤 | 账户订单中位数 | 最高账户订单 |
|---|---:|---:|---:|---:|---:|---:|
| 前缀 | 22 | 1072.2789% | 530.8950% | -18.9984% | 100 | 137 |
| 留一 | 22 | 1070.8368% | 94.9188% | -19.0467% | 100 | 130 |
| 逐一加入 | 39 | 1237.1641% | 965.8953% | -18.3885% | 101 | 130 |
| 随机子集 | 750 | 117.1891% | -12.1056% | -21.2073% | 23 | 143 |
| 顺序置换 | 150 | 1080.8378% | 1080.8378% | -15.9248% | 100 | 100 |

随机子集回撤严重度的 90 分位为 18.8486%，最差为 21.2073%；随机子集账户订单 90 分位为 48 笔，全场景最高为 143 笔。9→10 股财富变化为 -2.6452%，最差相邻扩展为 -16.8528%，最差逐一加入为 -16.8025%。门禁通过只说明本次冻结数据与场景边界内满足约束，不能声称所有任意子集都低于 18% 回撤。

## 六、被否决的方案

以下方案经过真实回测后未进入默认配置：

- 对所有 `TRANSITION` 永久缩仓：显著降低 22 股收益并增加交易；
- 把弱市策略复制到三个袖套：重复账本导致回撤和交易恶化；
- 进入弱市时清空已有趋势持仓：牛市回放收益大幅下降；
- 每日重新选择弱市龙头：形成隐性轮动；
- 动态持仓上限在无外部风险时生效：破坏正常牛市路径；
- 把风险篮子限制为用户当前股票池：导致风险证据随输入集合漂移；
- 为 13 股单池调成近似等权动态现金：13 股收益提高，但 22 股收益、回撤和交易明显恶化；
- 重新启用真实账户历史注入：混合账本、时间和挂单状态仍不安全，继续显式拒绝。

## 七、代码、测试与文档一致性

新增或更新的覆盖包括：连续风险升级、独立风险篮子、生产路由执行权、弱市监测模式、生产牛熊回放、阶段参数空间、交易三目标 Pareto、压力场景确定性、账户行业相对强度、pandas 3 类型兼容和 JSON 严格序列化。

维护模块 Pyright 已从现有错误修到零错误；Ruff、编译、完整单元测试、Bandit、依赖审计和五池精确回归均作为最终发布门禁。`README.md`、`BACKTEST_RESULTS.md`、本文件、黄金指标和回测工件使用同一组数字。

## 八、最终判断

本轮落实了共享审查中的生产逐日回放、连续风险升级、双证据过渡风控、三目标优化、分阶段搜索、股票池压力、动态袖套、趋势健康、自适应候选、独立行业篮子和账户行业相对强度。固定五池和 2024 熊市结果满足“不出现大幅负面影响”的验收原则，且多数池同时改善收益、回撤或交易。

仍需明确保留限制：750 个正式随机子集中最差收益为 -12.11%、最差回撤为 -21.21%；固定池、冻结数据与历史压力门禁都不是未来收益保证。后续验证应扩展更多年份、时间点可得的股票池和真实人工执行对账，而不是继续针对当前样本调参。

## 九、2026-08-16 风险治理轮（P0/P1 落实记录）

依据《trade 深度评估与长板强化改造报告》(2026-08-16)，本轮落实其全部 P0 与 P1 项。设计原则与验收原则一致：第一优先级是提高收益、降低回撤、减少交易次数；新增能力默认开启、识别到风险自动起作用；收益、回撤、交易次数只接受小幅波动，不允许大幅负面影响。

> **编号说明**：本章 P0/P1 编号指 2026-08-16 报告；第一至八章（2026-08-07 轮）沿用旧报告编号（如旧 P0-4=灾变冷却阻断再入场、旧 P1-2=子行业参数收缩）。两套编号互不相干；代码注释中凡引用 2026-08-16 报告的编号均带日期限定。

### 9.1 观测层与执行层的分离决策

P0-1/P0-2/P0-3 与 P1-1/P1-2/P1-3 全部实现为 `risk_governance.py` 纯函数与纯数据结构，随引擎默认开启。该层只读取既有状态做观测与输出，不进入任何交易决策路径。这一设计的验收含义：五池黄金指标在本轮改造前后逐位一致，熊市/震荡/牛市路径的行为完全不变，收益、回撤与交易次数零波动；风险识别质量的提升由 P0-4 晋级门在下一轮风险参数变更时强制兑现，而不是靠本轮隐式行为变化换取。

### 9.2 各项落实方式

| 报告项 | 实现 | 默认行为 |
|--------|------|---------|
| P0-1 Warmup Health Contract | `assess_warmup_health`：READY/DEGRADED/NOT_READY 三级报告，逐股预热天数、23 股参考篮就绪度、regime 证据新鲜度 | 随每次运行自动输出；日扫中 NOT_READY 自动抑制全部新增买入（fail-closed），DEGRADED 显著提示 |
| P0-2 Risk Event Classifier | `calibrate_risk_events`：独立检测已实现冲击（20 日内回撤 ≥8%），警报事件 1/3/5/10/20 日结果窗口与 Precision/Recall/领先天数/误报成本/漏报深度/牛市沉默率 | 每次回测自动附出 `risk_event_calibration` |
| P0-3 独立风险意见对象 | `RiskOpinion` / `build_risk_opinion`：等级、置信度、市场状态、牛市沉默、禁新开仓/冻结加仓语义、建议敞口上限、最弱簇、原因代码 | 每日自动生成；日扫完整打印，供人工与其他系统独立消费 |
| P0-4 宇宙压力尾部晋级门 | `stress_test_prefixes.py` 相对既有正式基线的强制晋级门 + 全排列不变性检查 | 风险层改动晋级前强制执行；无基线时建立基准并检查不变性 |
| P1-1 袖套分歧证据 | `compute_sleeve_agreement`：平均共识、3/2/1 袖套计数、部署率、最弱袖套、连续退化计数 | 每日自动采样；共识连续退化 ≥3 日进入风险意见原因代码，不新增状态机 |
| P1-2 风险篮覆盖置信度 | `basket_coverage_confidence`：0.45×观察比例 + 0.35×行业覆盖 + 0.20×持仓映射；overlay 新增 `coverage_metrics()` 审计接口 | 每日自动计算；置信度 <0.60 标记 `low_basket_coverage` 并降低意见强度 |
| P1-3 L1 冻结机会成本 | `calibrate_risk_events` 内 L1-only 警报段后 20 日收益中位数与 L1→L2 升级精度 | 每次回测自动附出 |

### 9.3 语义关键决策

- **预热健康分级**：regime 证据完全缺失 → NOT_READY（风险层失明，失败关闭）；存在但陈旧/历史不足 → DEGRADED（数据质量显式可见，不静默）。新上市股票不静默获得与成熟股票相同置信度，而是把整体降级为 DEGRADED 并列出名单。
- **冲击检测与召回语义**：冲击 episode 是"前视 20 日窗口能捕获 ≥8% 回撤"的日期段，冲击在其结束次日（兑现日）附近落地；警报成功检测要求开始不晚于兑现日且未在警告窗开始前过早结束；领先天数 = 兑现日与最早警报开始日之差。误报成本 = 警报开始后 20 日组合收益（正值即错过的上涨）。
- **空仓簿共识惯例**：三袖套全现金时共识记满值、最弱袖套置空、退化计数清零——全现金不是"共识退化"证据。
- **风险意见等级语义与 overlay 实际执行一致**：L1 冻结加仓、L2 禁新开仓、L3 建议敞口上限 50%，保证意见对象描述的就是系统真实会做的事。

### 9.4 验证与门禁

- `test_risk_governance.py` 26 项单元测试覆盖全部分级语义、校准指标、意见语义、覆盖度计算、袖套共识与 overlay 审计接口；全仓 321 项测试全部通过。
- 五池黄金指标回归 ALL PASS（整数精确、浮点 1-ulp 容差）；22 股池最大回撤的 1-ulp 差异经改动前代码复核确认为跨平台浮点求和顺序噪声，非行为漂移。CI 回归相应改为"整数精确 + 浮点 `rel_tol=1e-9`"，任何真实行为漂移仍会立即失败。
- P0-4 晋级门：固定前缀牛市财富 ≥99% 基线、随机子集回撤 P90/P95 恶化 ≤0.5 个百分点、全场景最差回撤/收益恶化 ≤1/2 个百分点、最差逐一加入财富下降 ≤3 个百分点、账户订单 P90/最差增加 ≤5/10 笔、风险减仓中位数增加 ≤2 笔、同 seed 全排列指标完全一致。
- 文档同步：README（风险治理章节、日扫字段、排查表、CI 语义）、BACKTEST_RESULTS（治理字段表、零漂移验证、质量门禁）与本文件使用同一套描述。
